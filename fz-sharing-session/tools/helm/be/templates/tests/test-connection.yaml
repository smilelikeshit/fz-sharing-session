apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "be.fullname" . }}-test-connection"
  labels:
    {{- include "be.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "be.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
